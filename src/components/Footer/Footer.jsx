import React from 'react';

const Footer = () => {
    return (
        <footer>
            {/* Agrega información de derechos de autor, enlaces, etc. */}
        </footer>
    );
};

export default Footer;
