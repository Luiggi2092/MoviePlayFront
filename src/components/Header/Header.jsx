import React from 'react';

const Header = () => {
    return (
        <header>
            {/* Aquí puedes agregar el logo de Netflix y elementos de navegación */}
        </header>
    );
};

export default Header;
